---
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: 90%

ignore:
  - "crates/mcp/telemetry.rs"  # feature-gated, requires OTel runtime
